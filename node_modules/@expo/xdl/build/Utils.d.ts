export declare function ncpAsync(source: string, dest: string, options?: any): Promise<unknown>;
export declare class Semaphore {
    queue: ((v: boolean) => void)[];
    available: number;
    acquire(): Promise<boolean>;
    release(): void;
}
